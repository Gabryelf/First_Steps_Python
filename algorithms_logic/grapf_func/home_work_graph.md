# 🏠 **ДОМАШНЕЕ ЗАДАНИЕ: "АНАЛИЗ СОЦИАЛЬНОЙ СЕТИ"**


## 🌐 **ЗАДАНИЕ : СОЗДАЙ СВОЙ ГРАФ**

**Цель:** Создайте граф для любой другой системы

```python
# ВАШ КОД ЗДЕСЬ:
# Создайте граф для одной из систем (на выбор):

# 1. Фанаты музыкальных групп
music_fans = {
    'Рок-фанат': ['Металлист', 'Панк'],
    'Металлист': ['Рок-фанат', 'Хэви-металлист'],
    # ... добавьте свои варианты
}

# 2. Связи между странами
countries = {
    'Россия': ['Китай', 'Казахстан', 'Беларусь'],
    'Китай': ['Россия', 'Казахстан', 'Индия'],
    # ... добавьте страны
}

# 3. Игроки в одной команде
team_players = {
    'Нападающий': ['Полузащитник', 'Вратарь'],
    'Защитник': ['Вратарь', 'Полузащитник'],
    # ... добавьте позиции
}

# Выберите один вариант и реализуйте функции для него
def analyze_your_graph(your_graph):
    """Анализируйте ваш граф"""
    print("📊 Анализ вашего графа:")
    print(f"Всего элементов: {len(your_graph)}")
    # Добавьте свой анализ
    
analyze_your_graph(ваш_граф)
```

---


## 🎓 **ЧТО ПРОВЕРЬТЕ ПЕРЕД СДАЧЕЙ:**

1. ✅ Весь код запускается без ошибок
2. ✅ Все функции возвращают правильные значения  
3. ✅ Вы добавили себя в сеть
4. ✅ Ваш код чистый и понятный
5. ✅ Вы протестировали все случаи

---

## 📤 **КАК СДАТЬ:**

1. **Скопируйте итоговый код** в файл `homework.py`
2. **Заполните все задания** (уберите `pass`, напишите свой код)
3. **Запустите и проверьте**, что всё работает
4. **Сдайте файл** преподавателю

**Удачи! Помните: практика - лучший способ научиться программировать!** 🚀

---

<div align="center">

## 🏆 **БОНУСНОЕ ЗАДАНИЕ (по желанию)**

**Создайте визуализацию графа!**

</div>

```python
# Установите: pip install networkx matplotlib
import networkx as nx
import matplotlib.pyplot as plt

def visualize_graph():
    G = nx.Graph()
    
    # Добавьте вершины и рёбра из вашего графа
    for person, friends in graph.items():
        for friend in friends:
            G.add_edge(person, friend)
    
    # Нарисуйте граф
    nx.draw(G, with_labels=True, node_color='lightblue', 
            node_size=2000, font_size=10)
    plt.title("Ваша социальная сеть")
    plt.show()

# Запустите
visualize_graph()
```
